# 系统架构与模块契约 (System Patterns)

> **[用途说明]**
> 本文件是新 Agent 接手项目时的**第一份必读文档**。
> 它回答三个问题：这个系统是什么、有哪些可用单元（含调用方式）、单元之间有哪些不可违反的规则。
> AI 在编写任何代码前，必须先查阅本文件，确认目标功能是否已有实现，避免重复造轮子。
> 如果发现新模块或新约束，必须通过 `[存档]` 指令更新此文件。

---

## 1. 系统定性

[TODO: 用一句话描述。格式：[系统类型：智能体 / 流水线 / 混合] — [核心用途] — 当前版本：[vX]]

示例：`混合型（状态机驱动的多智能体管线）— 全自动短视频从素材到成片的端到端生产系统 — 当前版本：V9`

---

## 2. 数据结构字典

> 项目中所有跨模块传递的核心数据结构。新 Agent 在调用任何单元前必须先查阅此表，确认输入输出格式。

[TODO: 逐一列出。如果某个结构尚未确认，用 [待确认] 标注具体字段。]

| 结构名 | 定义位置 | 关键字段 | 说明 |
|---|---|---|---|
| `[StructName]` | `path/to/file.py:LineN` | `field1: type, field2: type` | [一句话说明该结构的用途和生命周期] |

---

## 3. 可用单元清单

> 所有可直接调用的功能单元。**状态列是关键**：✅ 已完成可直接使用，🚧 开发中接口不稳定，❌ 未开始仅为存根。
> 新 Agent 必须优先复用 ✅ 状态的单元，不得重新实现已完成的功能。

[TODO: 逐一列出所有 skills/、src/、agents/ 下的核心模块。不能遗漏任何已完成的单元。]

| 单元名 | 位置 | 作用（一句话） | 输入类型 | 输出类型 | 外部依赖 | 状态 |
|---|---|---|---|---|---|---|
| `[UnitName]` | `path/to/entry.py` | [一句话描述职责] | `[InputStruct 或基础类型]` | `[OutputStruct 或基础类型]` | [API/服务名，无则填 `-`] | ✅ |

---

## 4. 数据流与包含关系

> 描述数据在各单元之间的流转路径。格式：`[单元A](输出结构) → [单元B](输出结构) → [最终产物]`
> 如果存在并行分支，用缩进表示。

[TODO: 根据入口文件和核心引擎的调用链推断，每个箭头对应一次单元调用。]

```
[输入源] 
  → [单元A]  产出: [中间产物A]
  → [单元B]  产出: [中间产物B]
      → [单元C]  产出: [最终输出]
```

---

## 5. 架构约束（不可违反）

> 这些规则是从代码中提取的硬性约束，违反会导致系统崩溃或数据损坏。
> 每条规则必须标注代码依据（文件路径）。

[TODO: 从 assert、raise、StateMachineError、TODO/FIXME 注释中提取。]

- **[约束名称]**：[具体要求] — 依据：`path/to/file.py`

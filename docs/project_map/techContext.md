# 技术栈与上下文约束 (Tech Context)

> **[用途说明]**
> 本文件记录系统使用的技术栈、第三方 API 约束、环境依赖以及已知的技术坑点。
> 当 AI 提议引入新库或调用 API 时，必须先检查此文件。

## 1. 核心技术栈 (Tech Stack)
[列出语言、框架、数据库等。]

*示例（视频智能体）：*
*- 语言：Python 3.10+*
*- 核心框架：Pydantic (数据校验), MoviePy (视频处理), FastAPI (接口)*

## 2. 外部依赖与 API (External Dependencies & APIs)
[列出依赖的外部服务及其限制。]

*示例：*
*- **LLM API**: 统一使用 Vertex AI Gemini 3.1 Pro，禁止直接调用 OpenAI API。*
*- **音频分离**: 使用 Demucs，需要本地 GPU 支持，处理 1 分钟音频约需 10 秒。*

## 3. 已知的技术坑点与约束 (Known Gotchas & Constraints)
[记录那些"不要这么做，因为之前踩过坑"的经验。]

*示例：*
*- **FFmpeg 内存泄漏**: 在处理 4K 视频时，直接使用 MoviePy 会导致内存 OOM，必须改用 subprocess 直接调用 ffmpeg 命令行。*
*- **路径约束**: 所有相对路径必须基于项目根目录计算，禁止使用基于当前脚本的相对路径。*

---
### 引导问题（新项目启动时请回答）：
1. 项目必须使用哪些特定的技术或框架？
2. 有哪些外部 API 或服务的速率限制/成本限制需要注意？
3. 开发环境和生产环境有什么区别？

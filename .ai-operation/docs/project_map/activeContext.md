# 当前工作焦点 (Active Context)

> **[用途说明]**
> 本文件是 AI 的短期工作记忆，记录当前会话的焦点任务、正在处理的问题和即将执行的下一步。
> **[DYNAMIC]** 每次会话开始时由 AI 读取，每次 `[存档]` 时由 AI 更新。
> 人类架构师无需手动维护此文件，完全由 AI 自动维护。

---

## 1. 当前焦点 (Current Focus)

> **填写规范**：一句话描述现在最重要的一件事。
> 格式：`正在 [动作] [目标]，目标是 [预期结果]。`
>
> **示例**：`正在开发字幕生成节点（SubtitleNode），目标是让它能处理中英双语并输出 .srt 文件。`

[由 `[初始化项目]` Phase 3.4 写入初始焦点，后续由 `[存档]` 自动更新]

---

## 2. 正在处理的问题 (Active Issues)

> **填写规范**：列出当前遇到的、**尚未解决**的技术问题或决策点。
> 已解决的问题移入 `progress.md` 的已完成里程碑，不保留在此。
>
> **示例**：
> - `SubtitleNode` 的 Whisper 输出时间戳与 FFmpeg 的帧时间戳有 ±0.3s 偏差，原因未定位
> - 尚未决定：字幕烧录是在 `SubtitleNode` 内完成，还是作为独立的 `BurnNode`？

无

---

## 3. 即将执行的下一步 (Immediate Next Steps)

> **填写规范**：列出接下来 1-3 件具体的、可立即执行的事，按优先级排序。
> 必须是可操作的指令，不能是模糊的方向。
>
> **示例**：
> 1. 运行 `python tests/test_subtitle.py` 复现时间戳偏差问题
> 2. 在 `skills/subtitle/main.py` 里加 `--word_timestamps True` 参数
> 3. 完成后执行 `[存档]`

1. 执行 `[初始化项目]` 完成项目接管
2. 填写 `systemPatterns.md` 的可用单元清单
3. 确认第一个待开发节点并创建 taskSpec

---

## 4. 最近的决策记录 (Recent Decisions)

> **填写规范**：记录最近做出的重要架构或实现决策，以及决策的理由。
> 只记录为什么这样做，而不是那样做的决策。
>
> **示例**：
> - **字幕烧录独立为 BurnNode**：决定将字幕烧录从 SubtitleNode 中分离，原因是烧录参数需要频繁调整，独立节点更容易单独测试和替换。

_暂无记录_

---

> **上次更新**：[由 `[存档]` 自动写入时间戳]
